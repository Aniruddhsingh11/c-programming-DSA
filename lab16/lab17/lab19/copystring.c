#include<stdio.h>
int main ()
{
    char i=0, src[100], dst[100];

    printf("enter source string\n");
    scanf("%s",src);

    while(dst[i] = src[i++]);

    printf("\n destination string %s",dst);
    return 0;
}