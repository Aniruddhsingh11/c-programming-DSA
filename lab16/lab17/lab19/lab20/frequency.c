#include<stdio.h>
#include<string.h>
int main ()
{
    char b[100], ch,i=0,count=0;
    printf("enter a string \n");
    gets(b);
    printf("\n enter a character to find frequency");
    scanf("%c",&ch);
    while(count+=ch==b[i],b[i++]);
    printf("frequency of %c= %d", ch, count);
    return 0;
}